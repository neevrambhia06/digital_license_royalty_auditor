{
  "name": "digital-license-royalty-auditor-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "chmod +x frontend/node_modules/.bin/vite || true && npm run build --prefix frontend",
    "install:frontend": "npm install --prefix frontend",
    "postinstall": "npm install --prefix frontend",
    "start": "npm run start --prefix frontend"
  }
}
